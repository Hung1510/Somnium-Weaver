name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    # windows-latest because the app is WPF and the test project references it
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: restore
        run: dotnet restore SomniumWeaver.sln

      - name: build
        run: dotnet build SomniumWeaver.sln --no-restore -c Release

      - name: test
        run: dotnet test SomniumWeaver.sln --no-build -c Release --verbosity normal
